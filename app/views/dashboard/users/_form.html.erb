<%= simple_form_for @user do |f| %>
  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.input :password_confirmation %>
  <%= f.input :locale, collection: [['English', 'en'] , ['Русский', 'ru']], selected: @user.locale %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>