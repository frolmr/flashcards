<% if @random_card %>
<p class="text-center"><%= t('main_question') %></p>
<p class="text-center"><%= image_tag(@random_card.image) %></p>
<p class="text-center">
  <em><%= @random_card.translated_text %></em>
</p>
<br>
<br>
<div>
  <%= simple_form_for :card, url: :check do |f| %>
    <%= f.input :original_text, label: t('translate_field_title') %>
    <%= f.hidden_field :card_id, value: @random_card.id %>
    <%= f.submit t('check_button_title'), class: 'btn btn-primary' %>
  <% end %>
</div>
<% else %>
<p><%= t('slogan') %></p>
<% end %>